services:
  - type: web
    name: guesser-game
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/guesser-game-1.0.0.jar
    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m
